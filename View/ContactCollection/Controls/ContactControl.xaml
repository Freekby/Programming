<UserControl x:Class="ContactCollection.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ContactCollection.Controls"
             xmlns:vm="clr-namespace:ContactCollection.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <vm:BooleanInverseConverter x:Key="BooleanInverse"/>
        
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="3"/>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                    <Setter Property="Foreground" Value="Red"/>
                    <Setter Property="Margin" Value="3"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel Grid.Column="2" Background="White" Margin="8">
            <Grid Margin="3, 15, 15,3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Image Source="C:\Users\скда\source\repos\Project333\jFLd-SJBw7Y.jpg" Width="100" Height="100" Grid.RowSpan="2"/>

                <StackPanel Grid.Column="1">
                    <Label Content="Name:"/>
                    <TextBox x:Name="NameTextBox"  IsReadOnly="{Binding IsEditMode, Converter={StaticResource BooleanInverse}}">
                        <TextBox.Text>
                            <Binding Path="CurrentContact.Name" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                <Binding.ValidationRules>
                                    <DataErrorValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                    <Label Content="Phone number:"/>
                    <TextBox x:Name="PhoneTextBox"  IsReadOnly="{Binding IsEditMode, Converter={StaticResource BooleanInverse}}" PreviewTextInput="PhoneNumberTextBox_PreviewTextInput" DataObject.Pasting="PhoneNumberTextBox_Pasting">
                        <TextBox.Text>
                            <Binding Path="CurrentContact.PhoneNumber" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                <Binding.ValidationRules>
                                    <DataErrorValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                    <Label Content="Email:"/>
                    <TextBox x:Name="EmailTextBox"  IsReadOnly="{Binding IsEditMode, Converter={StaticResource BooleanInverse}}">
                        <TextBox.Text>
                            <Binding Path="CurrentContact.Email" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                <Binding.ValidationRules>
                                    <DataErrorValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
